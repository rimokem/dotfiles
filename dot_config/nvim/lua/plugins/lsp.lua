return {
  "neovim/nvim-lspconfig",
  opts = {
    servers = {
      -- Ruff (Linter & Formatter / Language Server)
      ruff = {},

      -- ty (Type Checker & Language Server)
      ty = {},
    },
  },
}
